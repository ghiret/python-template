Run the `/init-project` skill to initialize this project from the python-template.

Gathers: project name (snake_case), description, author name/email, license, GitHub username/org, GitHub Pages preference.

Actions: Update pyproject.toml, rename src/ package, update README.md, update devcontainer config, update release-please configs, update workflows/mkdocs.yml, create LICENSE, create required directories, initialize git remote, run initial setup.
